{
  "name": "password-manager",
  "version": "0.0.0",
  "private": true,
  "description": "Self-hosted, zero-knowledge password manager (Phase 0: backend fundament).",
  "type": "module",
  "workspaces": [
    "shared",
    "server",
    "extension"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "dev:server": "npm run dev --workspace @pm/server",
    "dev:ext": "npm run dev --workspace @pm/extension",
    "build:ext": "npm run build --workspace @pm/extension"
  },
  "engines": {
    "node": ">=20"
  }
}
