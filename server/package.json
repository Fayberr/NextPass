{
  "name": "@pm/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Fastify + SQLite backend for the password manager (Phase 0).",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json && cp -r src/migrations dist/migrations",
    "test": "vitest run"
  },
  "dependencies": {
    "@pm/shared": "*",
    "better-sqlite3": "^11.3.0",
    "fastify": "^5.1.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "5.6.3",
    "vitest": "^2.0.0"
  }
}
