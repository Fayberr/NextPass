# extension/ — placeholder

Chrome / Opera GX Manifest V3 extension. **Phase 1+** (login items, master-password unlock,
sync, autofill) and **Phase 2** (passkey main-world shim + virtual FIDO2 authenticator).

Not started. Will consume `@pm/shared` for crypto + API types.
